package week2;

import java.util.*;
import java.util.stream.Collectors;

public class stream {
    public static void main(String[] args) {
        //문제3
//        List<Integer> numbers1 = Arrays.asList(1, 2, 3);
//        List<Integer> numbers2 = Arrays.asList(3, 4);
//
//        int max= numbers1.stream().flatMap(n1->numbers2.stream().map(n2->n1*n2)).max((n1,n2)->n1-n2).get();
//        int max=numbers1.stream().flatMap(n1->numbers2.stream().map(n2->new Integer[]{n1,n2})).mapToInt(array->array[1]*array[2]).max().orElse(0);
//        System.out.println(max);




    }
}
